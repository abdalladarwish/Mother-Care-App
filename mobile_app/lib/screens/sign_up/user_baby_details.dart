import 'package:flutter/material.dart';

class UserBabyDetails extends StatelessWidget {
  const UserBabyDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
